// This file is intentionally empty
// We'll implement proper authentication middleware later

// Export an empty config to prevent Next.js from running middleware on all routes
export const config = {
  matcher: []
};
